// Deprecated: legacy StorageTier values have been removed.
// This script is kept only to avoid breaking references; it no longer does anything.

console.log(
  "normalizeStorageTier.mjs is deprecated (no legacy tiers to normalize).",
);
